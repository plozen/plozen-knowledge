# 2026-05-29 RAG Schema Smoke Test

## Environment

- Server: `server-13`
- Container: `plozen-knowledge-postgres`
- Database: `plozen_knowledge`
- Extension: `pgvector`

## Applied SQL

```bash
docker exec -i plozen-knowledge-postgres psql -v ON_ERROR_STOP=1 -U plozen -d plozen_knowledge < db/migrations/001_rag_schema.sql
docker exec -i plozen-knowledge-postgres psql -v ON_ERROR_STOP=1 -U plozen -d plozen_knowledge < db/seeds/001_demo_knowledge.sql
docker exec -i plozen-knowledge-postgres psql -v ON_ERROR_STOP=1 -U plozen -d plozen_knowledge < db/smoke/001_similarity_search.sql
```

## Result Counts

```text
sources=4
chunks=10
latest_tool=manual_sql_smoke,matches=5
```

## Top Result

```text
rank=1
source_title=PLOZEN Knowledge MOC
chunk_index=1
cosine_distance=0.000000
preview=MVP 순서는 document_sources, document_chunks, search_audit_logs 스키마 생성 이후 Obsidian/Markdown ingest, embedding 저장, similarity search, MCP search_knowledge 연결이다.
```

## Notes

- Demo seed uses deterministic vectors to verify pgvector storage and cosine search without calling an external embedding API.
- Production ingest should replace deterministic vectors with real embeddings.
- Search smoke writes a `manual_sql_smoke` row to `search_audit_logs`.
